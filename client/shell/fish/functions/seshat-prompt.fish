# seshat fish prompt function — PLACEHOLDER. Contents are owned by the Stage 3 (c) spec.
# Must not assume an interactive shell: `seshat init fish` emits it before conf.d's guard.
function seshat-prompt
end

# seshat fish conf.d — PLACEHOLDER. Contents are owned by the Stage 3 (c) spec. The
# `status is-interactive` guard must stay the FIRST statement (see `seshat init fish`).
status is-interactive; or exit

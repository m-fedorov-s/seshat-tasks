# Zig client build artifacts
client/zig/.zig-cache/
client/zig/zig-out/
client/zig/zig-pkg/

# Server runtime data
# dev/seshat-dev-data.json is the owner's pre-Stage-2 dataset; kept local
dev/seshat-dev-data.json
dev/seshat-dev.db
dev/.user-id

# Local config (contains credentials)
config.yaml
server/config.yaml
# dev/client.json is generated by dev/seed.sh; holds the dev user's token
dev/client.json

# OS / editor noise
*.swp
.DS_Store

.roo
plans/
docs/

# Archived subagent-driven-development ledgers (see CLAUDE.md → Conventions)
sdd_archive/

# Go build binary
/server/seshat
/client/bot/seshat-bot

# Claude local settings
.claude/settings.local.json

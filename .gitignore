# Zig client build artifacts
client/zig/.zig-cache/
client/zig/zig-out/
client/zig/zig-pkg/

# Server runtime data + atomic-write temp files
server/seshat-data.json
seshat-data.json
.seshat-*.tmp
dev/seshat-dev-data.json
dev/.seshat-*.tmp

# Local config (contains the shared secret)
config.yaml
server/config.yaml

# OS / editor noise
*.swp
.DS_Store

.roo
plans/
docs/

# Archived subagent-driven-development ledgers (see CLAUDE.md → Conventions)
sdd_archive/

# Go build binary
/server/seshat
/client/bot/seshat-bot

# Claude local settings
.claude/settings.local.json
